# Prototype

I personally prefer yarn, so to install project dependencies

```shell
yarn install && yarn upgrade --latest

# if u want npm instead
npm i && npm update
```

then to run the entire dev env

```shell
yarn run dev

# npm
npm run dev
```
